let factorial = fun(n: Number): Number {
    if (n <= 1) {
        return 1;
    }

    n * factorial(n - 1)
}

println(string(factorial(5))); // should print 120
println(string(factorial(0))); // should print 1